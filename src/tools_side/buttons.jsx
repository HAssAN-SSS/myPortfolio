import './buttons.css'

export default function Buttons(props) {
    return (
            <button className='btn'><h2>{props.contant}</h2>
                {props.icon}
                div
            </button>
            
    )
}